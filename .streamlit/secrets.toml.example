# Copia este archivo a `.streamlit/secrets.toml` y llena los valores.
# NO COMMITEES secrets.toml a git (ya esta en .gitignore).
#
# En Streamlit Cloud, agrega estos mismos secrets en:
# App settings → Secrets (formato TOML identico).

TURSO_URL = "libsql://cavalletto-admin-xxxxx.turso.io"
TURSO_TOKEN = "eyJhbGciOi..."
